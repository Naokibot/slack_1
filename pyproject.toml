[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "kosen-slack-bot"
version = "0.1.0"
description = "KOSEN countdown, reminders and JMA disaster notifications for Slack"
requires-python = ">=3.12"
dependencies = [
  "slack-bolt>=1.22,<2",
  "httpx>=0.27,<1",
  "defusedxml>=0.7,<1",
  "python-dotenv>=1.0,<2",
  "tzdata>=2025.2",
]

[project.optional-dependencies]
test = [
  "pytest>=8,<10",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools.packages.find]
include = ["kosen_bot*"]
